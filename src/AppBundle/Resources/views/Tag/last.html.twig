{% extends 'AppBundle:Layouts:main.html.twig' %}

{% block title %}
    Список последних тегов
{% endblock %}

{% block styles %}
    <link href="{{ asset('bundles/app/style/tag/last.css') }}" rel="stylesheet" />
{% endblock %}

{% block content %}
    <div id="last_tag">
        <ul>
            {% for tag in tags %}
                <li>
                    <a href='{{ path('tag_page', { "slug" : tag.hashSlug }) }}'>{{ tag.hashTag|hashTag }}</a>
                </li>
            {% else %}

            {% endfor %}
        </ul>
    </div>
{% endblock %}
